const fs = require('fs');
const path = require('path');

let file = fs.readFile(path.resolve(__dirname + `../../../gmailer.config.json`), (err, data) => {
    if (err) throw new Error(err.message);
    else return data;
})

module.exports = file;

console.log(file)